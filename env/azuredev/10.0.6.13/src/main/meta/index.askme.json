{
    "settings" : {
        "number_of_shards" : 5,
        "number_of_replicas" : 2,
        "index.cache.query.enable": true,
        "analysis": {
            "analyzer": {
                "keyword_analyzer": {
                    "type": "custom",
                    "tokenizer": "keyword",
                    "filter": ["trim", "lowercase"]
                }
            }
        }
    }
}
